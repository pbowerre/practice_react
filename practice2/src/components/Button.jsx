const styles = {
  background: "red",
  color: "purple",
  height: "100vh",
};

const Button = () => {
  return (
    <div style={styles}>
      <p>You Are Logged in</p>
    </div>
  );
};

export default Button;
